import 'package:flutter/material.dart';


class DetalleFoto extends StatelessWidget {
  const DetalleFoto({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}